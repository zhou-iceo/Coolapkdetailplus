<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        tools:replace="android:appComponentFactory"
        tools:ignore="UnusedAttribute"
        android:appComponentFactory="android.app.AppComponentFactory"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoolApkDetailplus">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LibXposed API 102 metadata -->
        <meta-data
            android:name="libxposed.module.api"
            android:value="102" />
        <meta-data
            android:name="libxposed.module.name"
            android:value="CoolapkDetailPlus" />
        <meta-data
            android:name="libxposed.module.description"
            android:value="酷安内容详情页搜索增强插件" />
        <meta-data
            android:name="libxposed.module.entry"
            android:value="com.atxz.coolapkdetailplus.LibXposedEntry" />

        <!-- Standard Xposed metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="酷安内容详情页搜索增强插件" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

    </application>

</manifest>
